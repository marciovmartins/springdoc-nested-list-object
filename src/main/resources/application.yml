spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/test"
    username: "root"
    password: "root"
  jpa:
    database: "mysql"
    show-sql: false
    properties:
      hibernate:
        format_sql: true
  mvc:
    throw-exception-if-no-handler-found: true
  web:
    resources:
      add-mappings: false
server:
  servlet:
    encoding:
      force: true